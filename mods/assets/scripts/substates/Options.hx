function onCreatePost() 
{
	
}
	/*

	for (checkbox in grpTexts)
		{
			checkbox.fieldWidth = 618;
			for(i in 0...2) {
				var cb = new FlxSprite(checkbox.x+checkbox.fieldWidth + (i*30),checkbox.y).loadGraphic(Paths.image('menu/options/impastacheckbox'), true, 30, 30);
				cb.animation.add("button", [i+2], 24, false);
				cb.animation.play('button', true);
				add(cb);
			}
		}
	var throwawayStr:FlxText = new FlxText(-100,-100,-1,'A');
	throwawayStr.setFormat(Paths.font('vcr.ttf'), 22, FlxColor.WHITE, FlxTextAlign.LEFT, FlxTextBorderStyle.OUTLINE, FlxColor.BLACK);

	var underlay:FlxSprite = new FlxSprite(panelX - 5, optionStartY - 5).makeGraphic(676 + 10, (optionSpacing * grpOptions.members.length) + 5,FlxColor.BLACK);
	underlay.alpha = 0.5;
	underlay.zIndex = 1;
	add(underlay);
	grpOptions.zIndex = 2;
	trace([grpTexts.members.length, panelX]);
	descText.x = 466;
	descText.fieldWidth = 710;
	
//	for (checkbox in checkboxGroup)
//		{
			//checkbox.x = 1126;
			//checkbox.offset.set(0,0);
		//}
}
		*/